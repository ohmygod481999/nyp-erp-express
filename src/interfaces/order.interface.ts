export enum OrderStatus {
  CREATED = 'created',
  PEDNING = 'pending',
  SUCCESS = 'success',
}

export enum OrderItemStatus {
  CREATED = 'created',
  PROCESSING = 'processing',
  SUCCESS = 'success',
}